name = "nokazn-me-api"
main = "build/worker/shim.mjs"
compatibility_date = "2025-03-01"

[build]
command = "cargo install -q worker-build && worker-build --release"

[[rules]]
globs = ["**/*.wasm"]
type = "CompiledWasm"

[vars]
ALLOWED_ORIGIN = "https://nokazn.me"
